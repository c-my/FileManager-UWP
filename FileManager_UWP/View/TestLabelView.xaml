<Page
    x:Class="FileManager_UWP.View.TestLabelView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FileManager_UWP.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:FileManager_UWP.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    >

    <StackPanel>
        <TextBlock
            FontSize="50"
            Text="Test Label View" />
        <Button FontSize="20" Content="加" Click="Button_Click"></Button>
        <!--<ListView ItemsSource="{x:Bind Labels}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}"></TextBlock>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>-->

        <controls:LabelListControl ItemsSource="{x:Bind Labels}" Tapped="LabelListControl_Tapped" />
        <controls:LabelListControl ItemsSource="{x:Bind empty}" />

    </StackPanel>
</Page>
